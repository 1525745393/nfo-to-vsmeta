使用说明
（对应 nfo-to-vsmeta.1.0.py 修复版 / transfer.py 修复版）

1. 环境要求
Python 3.8 或更高版本

依赖说明：
- xml.dom.minidom 为 Python 标准库，无需安装；
- 可选依赖 Pillow（图片压缩功能）：pip install Pillow
  未安装 Pillow 时脚本自动跳过压缩，直接使用原图，不影响转换功能。

2. 配置文件说明
脚本（nfo-to-vsmeta.1.0.py）默认读取同目录 config.json，用户可通过该文件自定义脚本行为。
若配置文件不存在，首次运行会自动生成默认配置。

示例配置文件内容（config.json）：

{
    "directory": "./videos",
    "poster_suffix": "-poster.jpg",
    "fanart_suffix": "-fanart.jpg",
    "video_extensions": [".mkv", ".mp4", ".rmvb", ".avi", ".wmv", ".ts"],
    "delete_vsmeta": false,
    "max_workers": 4,
    "compress_image": true,
    "compress_kb": 200,
    "log_file": "process.log"
}

参数说明：
- directory: 指定需要扫描的文件夹路径，所有符合条件的文件都会被处理。
  示例值："./videos" 表示当前目录下的 videos 文件夹；也支持列表形式，如 ["./videos", "./movies"]。
- poster_suffix: 视频文件对应的海报文件后缀。
  示例值："-poster.jpg" 表示文件名为 [视频文件名]-poster.jpg。
- fanart_suffix: 视频文件对应的背景文件后缀。
  示例值："-fanart.jpg" 表示文件名为 [视频文件名]-fanart.jpg。
- video_extensions: 支持的视频扩展名列表。
  示例值：[".mkv", ".mp4"] 表示支持的文件扩展名为 .mkv 和 .mp4。
- delete_vsmeta: 是否先删除已有的 .vsmeta 文件再重新转换。
  true 表示删除后重新转换，false 表示保留（已转换的自动跳过）。
- max_workers: 多线程并发数，默认 4。
- compress_image: 是否压缩图片（需要安装 Pillow），默认 true。
- compress_kb: 图片压缩目标大小（KB），默认 200。
- log_file: 日志文件路径，默认 process.log。

3. 使用步骤
第一步：准备环境
确保已安装 Python 3.8 及以上版本。
下载 nfo-to-vsmeta.1.0.py 文件。
确保目标目录下包含 .nfo 文件和对应的海报、背景文件。

第二步：生成配置文件
首次运行脚本且未提供 config.json 文件时，脚本会自动生成默认配置文件 config.json。
修改生成的配置文件以满足具体需求（主要是 directory、poster_suffix、fanart_suffix）。

第三步：运行脚本
运行脚本并指定配置文件路径（如果需要）：

    python3 nfo-to-vsmeta.1.0.py --config config.json

如果未指定 --config 参数，脚本会默认从当前目录加载 config.json。

轻量版 transfer.py 的用法：

    python3 transfer.py --directory /volume1/video/Links/Movie/

也支持 --config、--poster、--fanart 参数覆盖配置。

4. 示例
示例 1：扫描默认目录并生成 .vsmeta 文件
假设文件目录结构如下：

    /videos
      ├── movie1.mp4
      ├── movie1.nfo
      ├── movie1-poster.jpg
      ├── movie1-fanart.jpg
      ├── movie2.mkv
      ├── movie2.nfo
      └── ...

运行脚本后，生成的 .vsmeta 文件如下：

    /videos
      ├── movie1.mp4
      ├── movie1.nfo
      ├── movie1-poster.jpg
      ├── movie1-fanart.jpg
      ├── movie1.vsmeta       # 自动生成
      ├── movie2.mkv
      ├── movie2.nfo
      ├── movie2.vsmeta       # 自动生成

5. 日志
脚本会将运行日志保存到 log_file（默认 process.log），并将关键日志输出到终端。

日志示例：

    2025-05-11 17:30:00 - INFO - 加载配置成功
    2025-05-11 17:30:01 - INFO - 删除已有 vsmeta 文件: /videos/movie1.vsmeta
    2025-05-11 17:30:02 - INFO - 成功创建 vsmeta 文件: /videos/movie1.vsmeta
    2025-05-11 17:30:03 - INFO - 本次处理完成：共 2 个文件成功生成 .vsmeta

6. 注意事项
- 确保 .nfo 文件格式正确，且与目标视频文件同名；
- 确保对应的海报和背景文件存在，否则会跳过相关处理；
- 如果启用了 delete_vsmeta，旧的 .vsmeta 文件将被删除，请谨慎操作；
- 重置元数据后，需在群晖 设置 > 视频库 > 再次搜索所有视频信息 刷新缓存。

7. 高级功能
1. 多线程加速：通过 max_workers 配置并发线程数，默认 4。
2. 多目录扫描：directory 配置为列表即可一次处理多个路径。
3. 自定义配置文件：通过 --config 参数指定任意路径的配置文件。
4. 自动生成默认配置：首次运行时，如果找不到配置文件，脚本会自动生成。
5. 图片压缩：安装 Pillow 后自动将海报/背景图压缩至 compress_kb（默认 200KB）以内，减小 vsmeta 体积。
