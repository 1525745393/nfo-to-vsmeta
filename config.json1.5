{ // 扫描的视频根目录（支持一个路径或列表文件） "root": "/volume2/NAS2/nas媒体自动管理/待整理/JAV_output",

// 是否从文件读取路径列表（可选） // "root_file": "paths.txt",

// 线程数量，建议设为 CPU 核心数 "threads": 4,

// 是否删除原始 .nfo 文件，true = 删除，false = 保留 "delete_nfo": false,

// 是否仅转换缺失的 .vsmeta 文件（跳过已有的） "only_missing": true,

// 是否覆盖已有的 .vsmeta 文件，默认 false "overwrite_vsmeta": false,

// 日志输出目录（为空时为当前目录） "log_dir": "",

// 是否启用重命名视频文件 "enable_rename": false,

// 是否跳过已命名规范的文件 "skip_renamed": true,

// 是否保留原文件名为备份（如 *.bak） "backup_original": true,

// 视频重命名规则："title" 表示使用标题；"id" 表示使用番号（默认） "rename_mode": "id",

// 自定义 .vsmeta 输出目录，留空表示与视频同目录 "output_vsmeta_dir": "",

// 自定义封面、背景图输出目录，留空表示与视频同目录 "output_image_dir": "" }

